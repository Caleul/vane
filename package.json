{
  "name": "@lilka/vane",
  "version": "0.0.0",
  "description": "Reference implementation of the Entity Event model and framework.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "clean": "node -e \"for (const path of ['dist', 'dist-test']) require('node:fs').rmSync(path, { recursive: true, force: true })\"",
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.build.json",
    "check": "biome check . && tsc --noEmit",
    "test": "node -e \"require('node:fs').rmSync('dist-test', { recursive: true, force: true })\" && tsc -p tsconfig.test.json && node --test 'dist-test/test/**/*.test.js'",
    "verify": "npm run check && npm run build && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Caleul/vane.git"
  },
  "license": "MIT",
  "dependencies": {
    "typescript": "5.7.3"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "22.10.5"
  }
}
